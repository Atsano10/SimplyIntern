name: Daily Refresh

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger job refresh
        run: |
          curl -X POST https://mijakorauzqwzfffvcwb.supabase.co/functions/v1/dynamic-worker \
            -H "Content-Type: application/json"
